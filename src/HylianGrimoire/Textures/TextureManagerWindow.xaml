<Window
    x:Class="HylianGrimoire.Textures.TextureManagerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Hylian Grimoire - Texture Manager">

    <Grid
        Padding="20"
        RowSpacing="16"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Orientation="Horizontal"
            Spacing="12"
            VerticalAlignment="Center">
            <TextBlock
                Text="Texture Manager"
                Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock
                x:Name="ProfileText"
                Text="Load a ROM."
                Style="{ThemeResource CaptionTextBlockStyle}"
                VerticalAlignment="Center" />
        </StackPanel>

        <Grid
            Grid.Row="1"
            ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Padding="16"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Textures"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <Button
                            x:Name="ExportFolderButton"
                            Grid.Column="1"
                            Content="Export folder"
                            ToolTipService.ToolTip="Export every ROM texture as PNG files to a folder, using Hylian Grimoire's texture folder layout and names."
                            Click="OnExportFolder" />

                        <Button
                            x:Name="ReplaceFolderButton"
                            Grid.Column="2"
                            Content="Replace folder"
                            ToolTipService.ToolTip="Replace ROM textures using PNG files from an exported texture folder. PNG size and format must match the original texture."
                            Click="OnReplaceFolder" />
                    </Grid>

                    <TreeView
                        x:Name="TextureTree"
                        Grid.Row="1"
                        Expanding="OnTextureTreeExpanding"
                        SelectionChanged="OnTextureSelectionChanged" />
                </Grid>
            </Border>

            <Border
                Grid.Column="1"
                Padding="16"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Preview"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <Button
                            x:Name="ExportButton"
                            Grid.Column="1"
                            Content="Export PNG"
                            Click="OnExportTexture" />

                        <Button
                            x:Name="ReplaceButton"
                            Grid.Column="2"
                            Content="Replace PNG"
                            Click="OnReplaceTexture" />
                    </Grid>

                    <Grid
                        Grid.Row="1"
                        ColumnSpacing="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Details"
                            Style="{ThemeResource BodyStrongTextBlockStyle}" />

                        <TextBlock
                            x:Name="DetailsText"
                            Grid.Column="1"
                            Text="Select a texture."
                            TextWrapping="Wrap" />
                    </Grid>

                    <Border
                        x:Name="PreviewViewport"
                        Grid.Row="2"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Padding="16"
                        SizeChanged="OnPreviewViewportSizeChanged">
                        <Grid
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <Image
                                x:Name="PreviewAlphaImage"
                                Stretch="None" />
                            <Image
                                x:Name="PreviewImage"
                                Stretch="None" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="2"
            Style="{ThemeResource CaptionTextBlockStyle}"
            TextWrapping="Wrap"
            Text="PNG replacement must match the selected texture's exact pixel size." />

        <Grid
            x:Name="ProgressOverlay"
            Grid.RowSpan="3"
            Background="#99000000"
            Visibility="Collapsed"
            Canvas.ZIndex="10">
            <Border
                Width="340"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        x:Name="ProgressTitleText"
                        Text="Exporting textures"
                        Style="{ThemeResource SubtitleTextBlockStyle}"
                        HorizontalAlignment="Center" />

                    <TextBlock
                        x:Name="ProgressPercentText"
                        Grid.Row="1"
                        Text="0%"
                        Style="{ThemeResource BodyStrongTextBlockStyle}"
                        HorizontalAlignment="Center" />

                    <ProgressBar
                        x:Name="ProgressBar"
                        Grid.Row="2"
                        Height="4"
                        Minimum="0"
                        Maximum="100"
                        Value="0" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
