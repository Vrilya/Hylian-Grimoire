<Window
    x:Class="HylianGrimoire.Glyphs.GlyphRemapWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Hylian Grimoire - Glyph Remap">

    <Grid
        Padding="20"
        RowSpacing="16"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Text="Glyph Remap"
            Style="{ThemeResource TitleTextBlockStyle}" />

        <TextBlock
            Grid.Row="1"
            MaxWidth="560"
            TextWrapping="Wrap"
            Text="Replace one glyph byte with another in the currently loaded script. ROM language banks are handled by the active loaded language only." />

        <Grid Grid.Row="2" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Padding="16"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Replace" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <ComboBox
                        x:Name="SourceCombo"
                        MinWidth="230"
                        SelectionChanged="OnSelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Image
                        x:Name="SourceImage"
                        Width="64"
                        Height="64"
                        Stretch="Uniform"
                        HorizontalAlignment="Left" />
                    <TextBlock x:Name="SourceCountText" />
                </StackPanel>
            </Border>

            <Border
                Grid.Column="1"
                Padding="16"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="With" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <ComboBox
                        x:Name="TargetCombo"
                        MinWidth="230"
                        SelectionChanged="OnSelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Image
                        x:Name="TargetImage"
                        Width="64"
                        Height="64"
                        Stretch="Uniform"
                        HorizontalAlignment="Left" />
                    <TextBlock x:Name="TargetCountText" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="3">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="Cancel" Click="OnCancel" />
                <Button
                    x:Name="ApplyButton"
                    Content="Apply"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnApply" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
