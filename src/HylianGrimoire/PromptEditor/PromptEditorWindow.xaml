<Window
    x:Class="HylianGrimoire.PromptEditor.PromptEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Hylian Grimoire - Prompt Editor">

    <Grid
        Padding="20"
        RowSpacing="16"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Orientation="Horizontal"
            Spacing="12"
            VerticalAlignment="Center">
            <TextBlock
                Text="Prompt Editor"
                Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock
                x:Name="ProfileText"
                Text="Load a supported retail ROM."
                Style="{ThemeResource CaptionTextBlockStyle}"
                VerticalAlignment="Center" />
        </StackPanel>

        <Grid
            Grid.Row="1"
            ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Spacing="12">
                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock
                            Text="Controls"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <ListView
                            x:Name="PromptList"
                            Height="160"
                            SelectionChanged="OnPromptSelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Label}" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid
                        RowSpacing="12"
                        ColumnSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            x:Name="SelectedPromptText"
                            Grid.ColumnSpan="2"
                            Text="Select a prompt"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <NumberBox
                            x:Name="IconXBox"
                            Grid.Row="1"
                            Header="Button X"
                            Minimum="-160"
                            Maximum="160"
                            SpinButtonPlacementMode="Compact"
                            ValueChanged="OnCoordinateChanged" />

                        <NumberBox
                            x:Name="TextXBox"
                            Grid.Row="1"
                            Grid.Column="1"
                            Header="Text X"
                            Minimum="-160"
                            Maximum="160"
                            SpinButtonPlacementMode="Compact"
                            ValueChanged="OnCoordinateChanged" />

                        <Button
                            Grid.Row="2"
                            Content="Reset selected"
                            HorizontalAlignment="Stretch"
                            Click="OnResetSelected" />

                        <Button
                            Grid.Row="2"
                            Grid.Column="1"
                            Content="Reset all"
                            HorizontalAlignment="Stretch"
                            Click="OnResetAll" />
                    </Grid>
                </Border>
            </StackPanel>

            <Border
                Grid.Column="1"
                Padding="16"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Preview"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <ToggleButton
                            x:Name="FramesButton"
                            Grid.Column="1"
                            Content="Frames"
                            Checked="OnPreviewInputChanged"
                            Unchecked="OnPreviewInputChanged" />

                        <ToggleButton
                            x:Name="GuidesButton"
                            Grid.Column="2"
                            Content="Guides"
                            Checked="OnPreviewInputChanged"
                            Unchecked="OnPreviewInputChanged" />
                    </Grid>

                    <Border
                        Grid.Row="1"
                        Background="Black"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                        <Image
                            x:Name="PreviewImage"
                            Stretch="None" />
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <StackPanel
            Grid.Row="2"
            Spacing="4">
            <TextBlock
                Style="{ThemeResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"
                Text="Uses the ROM language selected in the main window." />
            <TextBlock
                x:Name="StatusText"
                Style="{ThemeResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
        </StackPanel>
    </Grid>
</Window>
